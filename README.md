# 🏗️ RE-DCF-Tool — 都更/危老前期評估工具

> 永盛開發建設內部工具。輸入土地與**逐層**建築參數，自動完成容積查核（建築技術規則 §162）、銷售坪效、開發評效，並偵測六大法規踩坑點。  
> **圖說（面積表）為唯一真實依據**，工具逐層偵測免計超出並補計，不蓋過建築師圖說。

🔗 **線上使用：[re-dcf-tool-ovmbnrh45ew2khaklhhn3t.streamlit.app](https://re-dcf-tool-ovmbnrh45ew2khaklhhn3t.streamlit.app)**

---

## 📊 現況（v4）

| 項目 | 狀態 |
|------|------|
| 版本 | v4 UI 重設計版 |
| 部署 | ✅ Streamlit Community Cloud（`main` 分支自動部署） |
| 黃金測試 | ✅ 全 PASS（合成案例 A–D + L6 鎖定 + JSON 合約；`pytest` 一鍵） |
| 待建築師確認 | ⚠️ 梯廳基準 5% 或 8%？陽台基準 10% 或 15%？ |

---

## ✨ 功能（v4）

### 容積查核（L1–L4）
- **逐層表格**：每層樓板 / 計容積 / 梯廳 / 安全梯 / 陽台實填，可勾選排除（B1F 防空避難室 §117）
- **Excel / CSV 一鍵匯入**面積表，自動對應欄名
- **容積量表 + 結論橫幅**：一眼看出是否超出允建容積
- **三項免計逐層查核**：梯廳 / 安全梯 / 陽台（含 1/8 投影），超出自動補計入容積

### 坪效與財務（L4.5–L5）
- **銷售坪效推導**：允建容積 → 室內坪 → 銷售坪 → 銷坪比（正常 1.58–1.68）
- **開發評效快篩**：總銷 / 總成本（> 5 優良 / 2–5 可行 / < 2 偏低）
- **雙軸敏感度熱力圖**：公設比 × 售價

### 都更全案投報（L6）
- **共同負擔六大科目**（都市更新權利變換實施辦法）：工程 A / 管維 B / 權變 C / 利息 D / 稅捐 E / 管理 F
- **地主分回 / 共負比 / 報酬率**
- **售價 × 營造敏感度熱力圖**
- 另附 `都更全案投報_對照範本.xlsx` 供開啟舊 Excel 坪效分析表逐格對照

### UI（v4 新）
- 精簡 **4 欄 KPI**：允建容積 / 容積餘量 / 銷坪比 / 投報率
- **5 個 Tab**：① 容積查核　② 坪效分析　③ 財務評效　④ 都更全案投報　⑤ 報告匯出
- **Sidebar 雙欄輸入**，捲動高度縮減約 40%
- **全域 CSS**：Metric 卡底色、Tab 膠囊樣式、表頭底色、頁面留白
- **一頁報告匯出**（Markdown / CSV）

---

## 🚀 本機執行（P1-3：Python 3.11，版本已釘死）

```bash
pip install -r requirements.txt
streamlit run app.py        # 啟動 Web UI（預設埠 8501；Streamlit Cloud 部署同此進入點）
pytest                      # 黃金測試一鍵（每次改公式必跑；等同 python test_golden.py）
python min_example.py       # 最小可跑範例：讀合成 JSON → core 計算 → 印出合約 result
python make_template.py     # 重新產生 Excel 對照範本
python schemas/examples/generate_examples.py   # 重新產生合約範例 JSON
```

> **合併後定位（P2 README 瘦身標記）**：本 README 之「核心計算邏輯」「黃金測試」
> 段落合併後保留；「專案定位／導覽」讓給合併庫總 README。啟動路徑並列時，
> 本半邊＝「計算工具（Streamlit）」，另一半＝「靜態站（GitHub Pages）」。

---

## 🧮 核心計算邏輯（§162，全部逐層）

| 層 | 計算 |
|----|------|
| L2 基準容積 FA | `(基地面積 − 人行廣場) × 容積率` |
| L2 允建容積 | `FA × (1 + 獎勵率) + 容積移轉` |
| L3 梯廳超出 | `Σ 各層 max(0, 梯廳 − 樓板 × 梯廳免計%)` |
| L3 陽台超出（免計%） | `Σ 各層 max(0, 陽台 − 樓板 × 陽台免計%)` |
| L3 陽台超出（1/8投影） | `Σ 各層 max(0, 陽台 − 樓板 × 1/8)` |
| L3 安全梯上限 | `允建容積 × 15%` |
| L4 計入容積(修正後) | `計入容積(圖說) + 梯廳超出 + 陽台超出` |
| L4 容積餘量 | `允建容積 − 計入容積(修正後)`（< 0 = 超出需調整）|
| L4.5 銷售坪數 | `(允建容積 + 陽台免計) × 外皮係數 / 3.3058 / (1 − 公設比)` |
| L6 共同負擔 | `A工程 + B管維 + C權變 + D利息 + E稅捐 + F管理` |

> ⚠️ **逐層法才是正解**：陽台/梯廳超出必須各層樓板×% 判斷，用「總量 vs FA×%」會把超出量高估約 10 倍（實案驗證，紀錄私有）。

---

## ✅ 黃金測試（三案基準值）

| 案件 | 關鍵數字 |
|------|---------|
| 案例A（都更・全案管理） | 允建 7,616.00 m²、計入 7,616.20 m²、餘量 −0.20 m²（打滿邊界） |
| 案例B（危老） | 允建 4,177.92 m²、梯廳超出合計 30.80 m²、計入 4,210.80 m²、餘量 −32.88 m² |
| 案例C（防災都更） | 允建 4,017.60 m²、**陽台超出 29.40 m²**（逐層法）、計入 4,049.52 m²、餘量 −31.92 m² |
| L6 鎖定 | 案例D 共負比 66.00%／報酬率 51.50%；案例A 共負比 55.92%／報酬率 78.82% |

> 全部為**合成案例**（虛構輸入→決定性輸出，純迴歸鎖定）；費率曾以真實案私下校準（±5% 內），紀錄不進版控。

---

## 📂 檔案說明

| 檔案 | 說明 |
|------|------|
| `app.py` | 主程式（848 行，計算層 L1–L6 + Streamlit UI） |
| `test_golden.py` | 黃金迴歸測試（pytest 一鍵；合成案例 A–D + L6 鎖定 + JSON 合約） |
| `make_template.py` | 產生 Excel 對照範本（`openpyxl` 公式 1:1 對齊） |
| `都更全案投報_對照範本.xlsx` | 坪效及獲利分析對照範本（黃色=輸入，藍色=指標） |
| `投報分析檢核.md` | 都更全案投報內部方法論手冊 |
| `REVIEW.md` | Agent 2 審查記錄（陽台 Bug 修正、待確認參數） |
| `CLAUDE.md` | AI 助理開發規格（函式目錄、計算架構、踩坑點） |
| `requirements.txt` | 四個依賴套件 |
| `.streamlit/config.toml` | 主題（紫色 #534AB7）+ Server 設定 |
| `供py/` | ⚠️ .gitignore 排除，真實案件 Excel，**不可 commit** |

---

## ⚠️ 六大踩坑點

1. **梯廳 §162-1 逐層**：各層樓板 × 5% 或 8%，**不是** FA × 8% 總量
2. **陽台超出必須補計入**：超出 10% 的部分計入容積，不可寫 0
3. **基地面積用使照面積**：非謄本面積（實案曾見兩者差約 10%，用錯會墊高免計上限）
4. **三來源比對**：建築師 / 代書 / 建設公司數字需「圖說驗核區」比對
5. **B1F 防空避難室排除**：排除樓地板合計（§117），逐層表格取消勾選
6. **安全梯 15% 基準是「允建容積」**：非 FA

---

*永盛開發建設內部工具　｜　v4 UI 重設計版　｜　開發分支：`claude/claude-md-docs-ls9Bu`　→　部署：`main`*
