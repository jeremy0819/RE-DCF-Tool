# RE-DCF-Tool — 都更/危老前期評估工具

## 專案目的

Streamlit 互動式工具，將永盛開發建設「建築坪效與前期評估」Excel 財務模型轉化為程式。
輸入土地與建築參數 → 自動完成容積查核、銷售坪效計算、開發評效分析，並偵測法規踩坑點。

開發者背景：建築學碩士 + 都更開發 PM，無正式程式經驗，正在學 Python。
程式碼要求：清晰、大量中文註解、變數可用中文、優先可讀性而非簡潔。

## 技術棧

- Streamlit（前端 + 後端，單一 `app.py`）
- pandas / numpy（計算）
- plotly（圖表）
- 部署目標：Streamlit Community Cloud（免費）
- 執行：`streamlit run app.py`

## 五層計算架構（工具骨架）

- **L1 輸入層**：14 個欄位（放 sidebar）
- **L2 容積**：`FA = 基地使用 × 容積率`；`允建容積 = FA × (1+獎勵率) + 容積移轉`
- **L3 三項免計**：梯廳（各層樓板×%，逐層）、安全梯（允建容積×15%）、陽台（FA×10%）
- **L4 容積帳**：`計入容積 = 一樓 + 標準層×層數 + 梯廳超出×層數 + 陽台超出`；`容積餘量 = 允建 − 計入`
- **L4.5 銷售坪效**：銷坪比正常 1.58–1.68
- **L5 開發評效**：`開發評效 = 總銷 ÷ 總成本`（>5 優良 / 2–5 可行 / <2 偏低，永盛內部定義）

## 六大踩坑點（程式須防呆）

1. 梯廳 §162-1 是逐層 %（各層樓板 × 5% 或 8%），不是 FA×8% 總量
2. 陽台超出 10% 必須補計入容積，不可寫 0
3. 基地面積用「使照面積」非謄本面積（中正段謄本1,090 vs 使照983）
4. 三來源（建築師/代書/建設公司）數字需「圖說驗核區」比對
5. B1F 防空避難室排除樓地板合計（§117）
6. 安全梯 15% 基準是「允建容積」非 FA

## 黃金測試（須通過 `python test_golden.py`）

| 案 | 關鍵期望值 |
|----|-----------|
| 安和段 | 允建 7768.51、安全梯 1165.28、銷坪比 1.62 |
| 龜山段 | 允建 4244.04、梯廳每層超出 2.32、14層合計 32.45 |
| 中正段 | FA 2211.75、允建 4167.08、安全梯 625.05、陽台超出 29.11（逐層法）|

> ⚠️ 中正段兩處已修正（詳見 `REVIEW.md`）：
> 1.「獎勵率」由原 spec 的 0.6764 修正為 **0.8841**（防災都更 +88.4%）。
> 2.「陽台超出」由舊 spec 的 FA×10% 總量法 ~303 修正為 **逐層法 29.11**（差約 10 倍，逐層法才符 §162 與圖說；test_golden.py 斷言此值）。

## 程式撰寫規範

- 計算邏輯獨立成函式（`calc_*`），不碰 Streamlit，方便測試
- 變數用中文，函式名中英混用
- 每個計算區塊加中文註解，標明對應層級（L2/L3…）與法規
- 超出→`st.error()`(紅)、接近→`st.warning()`(黃)、合規→`st.success()`(綠)
- 數字顯示小數 2 位，面積 m²、坪數「坪」
- 絕不寫死結果，全部從 L1 推導

## Agent 框架

1. 撰寫 → 2. 審查（見 `REVIEW.md`）→ 3. 部署

## 第二階段（先不做）

住宅工具上線後，複製架構做商辦版（NOI + Cap Rate + 持有期報酬），另開對話處理。
