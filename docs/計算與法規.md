# 計算架構與法規知識（domain 參考檔）

> 從 CLAUDE.md v4.9 抽出的長內容。改公式前必讀本檔；改完必跑 `pytest`。
> 函式實作位置：`core/` package（v4.7 起）。本檔不寫行號（易過時），用函式名定位（grep 即可）。

---

## 計算架構（六層骨架）

| 層 | 名稱 | 關鍵公式 |
|----|------|---------|
| L1 | 輸入層 | 基地/容積/免計基準/成本/都更全案投報參數 |
| L2 | 容積 | `FA = 基地使用 × 容積率`；`允建容積 = FA × (1+獎勵率) + 容積移轉` |
| L3 | 三項免計 | 梯廳逐層 %、安全梯（允建×15%）、陽台逐層 10%（**非** FA 總量） |
| L4 | 容積帳 | `計入容積 = 圖說計入 + 梯廳超出 + 陽台超出`；`餘量 = 允建 − 計入` |
| L4.5 | 銷售坪效 | `銷售坪 = (允建容積+陽台免計)×外皮係數 / 3.3058 / (1−公設比)`；銷坪比 1.58–1.68 正常 |
| L5 | 開發評效 | `評效 = 總銷 / 總成本`（>5 優良 / 2–5 可行 / <2 偏低） |
| L6 | 都更全案投報 | 總銷 → 共同負擔六大科目 → 共負比 / 地主分回 / 報酬率 |
| L7 | 更新前估值 | `calc_更新前價值`（基礎版，未校準，B 級數字） |

## L6 共同負擔六大科目（都市更新權利變換實施辦法）

| 科目 | 計算方式 |
|------|---------|
| A 工程費用 | 營造費 + 設計監造（營造×5%）+ 工程管理（營造×3%） |
| B 管維費用 | A × 1% |
| C 權變費用 | 房地銷×1.5% + 戶數×拆補費 + 戶數×月租×月數 |
| D 貸款利息 | 土融利息 + 建融利息 |
| E 稅捐 | 房地銷×5%（營業稅）+ 總銷×0.1%（印花） |
| F 管理費用 | 總銷×5%（實施者服務費） |
| 合計 | `共同負擔 = A+B+C+D+E+F` |

## 預設費率（`財務率預設`，core/finance.py）——🔴 真實案校準成果，改動需使用者核准

| 參數 | 預設值 | 說明 |
|------|--------|------|
| 設計監造費率 | 5% | 佔營造費 |
| 工程管理費率 | 3% | 佔營造費 |
| 管維費率 | 1% | 佔工程費 A |
| 建融利率 / 年期 | 3.25% / 3 年 | |
| 營業稅 / 印花 | 5%（房地銷）/ 0.1%（總銷） | |
| 管理費率 | 5% | 佔總銷（實施者） |
| 拆補費 | 9 萬/戶 | |
| 租金補貼 | 2,000/月 × 42 月 | 都更期間 |

## 核心函式目錄（core/，以函式名 grep 定位）

| 函式 | 模組 | 說明 |
|------|------|------|
| `calc_容積查核` / `check_bonus_limit` / `calc_獎勵率合計` | capacity.py | 逐層 §162 免計查核、獎勵驗核（L2–L4） |
| `calc_坪效` / `calc_開發評效` | efficiency.py | L4.5–L5 |
| `calc_總銷` / `calc_共同負擔` / `calc_分回` / `calc_投報全案` / `calc_投報敏感度` / `財務率預設` | finance.py | L6 |
| `calc_更新前價值` | valuation.py | L7 基礎版 |
| `build_project_json` / `build_result_json` / `SCHEMA_VERSION` | contract.py | 對外合約（中文 calc → 英文 key 分界點） |
| `範本參數` / `範本樓層表` 等 | templates.py | 合成案例 A–D 種子 |
| `解析上傳` / `產生報告` | io.py | Excel/CSV 解析、Markdown 報告 |

## 六大踩坑點（程式須防呆——歷史上真的踩過）

1. **梯廳 §162-1 逐層**：各層樓板 × 5% 或 8%，**不是** FA×8% 總量
2. **陽台超出必須補計入**：超出 10% 的部分計入容積，不可寫 0
3. **基地面積用使照面積**：非謄本面積（實案曾差約 10%，用錯會墊高免計上限）
4. **三來源比對**：建築師/代書/建設公司數字需在「圖說驗核區」比對
5. **B1F 防空避難室排除**：排除樓地板合計（§117）
6. **安全梯 15% 基準是「允建容積」**：非 FA

## 黃金測試期望值（`pytest`；容差 0.5 m²）——🔴 期望值改動需使用者核准

| 案 | 關鍵期望值 |
|----|-----------|
| 案例A（都更・全案管理） | FA 4480.00、允建 7616.00、計入 7616.20、餘量 −0.20（打滿邊界設計） |
| 案例B（危老） | 允建 4177.92、梯廳超出 30.80、計入 4210.80、餘量 −32.88 |
| 案例C（防災都更） | FA 2160.00、允建 4017.60、陽台超出 29.40（**逐層法**）、梯廳超出 2.12、計入 4049.52、餘量 −31.92 |
| 案例D（危老・合建） | 允建 6120.00、計入 6157.50、餘量 −37.50 |
| L6 鎖定 | 案例D 共負比 66.00%／報酬率 51.50%；案例A 共負比 55.92%／報酬率 78.82% |

> 案例皆為合成（虛構輸入→決定性輸出，純迴歸鎖定）；費率曾以真實案私下校準（±5%），紀錄不進版控。

## 歷史修正教訓（詳見 REVIEW.md）

1. 「獎勵率」以「圖說獎勵容積 ÷ FA」精確反推，不可用 spec 粗值。
2. 「陽台超出」必須逐層法（各層樓板×10%），FA 總量法會高估約 10 倍。

## 待建築師確認的兩個參數（影響未來真實案件）

- 梯廳免計基準：5% 還是 8%？
- 陽台免計基準：§162 的 10% 還是 §162-3 的 15%？
