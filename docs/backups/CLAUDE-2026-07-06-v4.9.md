# RE-DCF-Tool — 都更/危老前期評估工具

## 專案目的

Streamlit 互動式工具，將永盛開發建設「建築坪效與前期評估」Excel 財務模型轉化為程式。
輸入土地與建築參數 → 自動完成容積查核、銷售坪效計算、開發評效分析，並偵測法規踩坑點。

開發者背景：建築學碩士 + 都更開發 PM，無正式程式經驗，正在學 Python。
程式碼要求：清晰、大量中文註解、變數可用中文、優先可讀性而非簡潔。

---

## 技術棧

| 套件 | 用途 |
|------|------|
| Streamlit ≥ 1.30 | 前端 + 後端，單一 `app.py` |
| pandas ≥ 2.0 | 樓層表格、CSV/Excel 解析 |
| plotly ≥ 5.18 | 儀表盤、瀑布圖、熱力圖 |
| openpyxl ≥ 3.0 | 讀寫 Excel（`make_template.py`） |

部署目標：Streamlit Community Cloud（免費，`main` 分支自動部署）。

**執行指令**：
```bash
pip install -r requirements.txt
streamlit run app.py        # 啟動 Web UI
python test_golden.py       # 黃金測試（每次改公式必跑）
python make_template.py     # 重新產生 Excel 對照範本
```

Windows 快速啟動：`啟動工具.bat`（呼叫 `streamlit run app.py`）。

---

## 檔案清單

> ⚠️ **v4.7 起架構重構**（見 ROADMAP.md）：計算公式全部搬入 `core/` package（Urban Renewal
> Core Engine）。`app.py` 僅 UI / Demo。`calc_engine.py` 降為相容 shim（`from core import *`），
> 既有 import 不變。本文件下方「函式目錄」的行號為 v4.6 舊版佈局，函式實作位置改見 `core/`。

```
RE-DCF-Tool/
├── core/                         # ★ 計算核心（唯一公式來源，Single Source of Truth）
│   ├── __init__.py               #   re-export 全部公開介面（__all__）
│   ├── capacity.py               #   容積查核 + 獎勵驗核（L2–L4）
│   ├── efficiency.py             #   銷售坪效 + 開發評效（L4.5–L5）
│   ├── finance.py                #   都更全案投報六大共負（L6）
│   ├── valuation.py              #   更新前價值估算（L7）
│   ├── contract.py               #   對外 Project JSON 合約（中文 calc → 英文 key）
│   ├── templates.py              #   範本案件種子資料（demo/測試）
│   └── io.py                     #   Excel/CSV 解析 + Markdown 報告
├── schemas/project_schema.json   # ★ 對外合約 JSON Schema（draft-07）
├── calc_engine.py                # 相容 shim（DEPRECATED；新程式請 from core import …）
├── law_db.py                     # 法規庫（→ 未來 regulation/ 分縣市）
├── app.py                        # 主程式：純 UI / Demo（不含計算邏輯）
├── test_golden.py                # 黃金迴歸測試（pytest；4 合成案 + L6 鎖定 + JSON 合約）
├── make_template.py              # 產生 Excel 對照範本
├── ROADMAP.md / CHANGELOG.md     # vNext 路線圖 / 版本變更紀錄
├── requirements.txt              # 部署依賴套件
├── 都更全案投報_對照範本.xlsx       # make_template.py 的輸出（可分享給建築師）
├── 啟動工具.bat                   # Windows 捷徑
├── CLAUDE.md                     # 本文件（AI 助理規格）
├── README.md                     # 使用者說明文件
├── REVIEW.md                     # Agent 2 審查記錄（已修正的陽台 Bug 等）
├── 投報分析檢核.md                  # 都更全案投報內部方法論手冊
├── .streamlit/config.toml        # 主題（紫色 #534AB7）與 server 設定
├── .gitignore                    # 排除 供py/（真實財務資料）、__pycache__
└── 供py/                         # ⚠️ .gitignore 排除，不可上傳：真實案件 Excel
```

**命名慣例（v4.7 拍板）**：內部 domain 函式/變數用中文（`calc_容積查核`）；
對外 JSON 合約 key 用英文（`allow_floor_area`）。分界點＝ `core/contract.py`。

---

## 計算架構（六層骨架）

| 層 | 名稱 | 關鍵公式 |
|----|------|---------|
| L1 | 輸入層 | 基地/容積/免計基準/成本/都更全案投報參數（sidebar） |
| L2 | 容積 | `FA = 基地使用 × 容積率`；`允建容積 = FA × (1+獎勵率) + 容積移轉` |
| L3 | 三項免計 | 梯廳逐層 %、安全梯（允建×15%）、陽台逐層 10%（**非** FA 總量） |
| L4 | 容積帳 | `計入容積 = 圖說計入 + 梯廳超出 + 陽台超出`；`餘量 = 允建 − 計入` |
| L4.5 | 銷售坪效 | `銷售坪 = (允建容積+陽台免計)×外皮係數 / 3.3058 / (1−公設比)`；銷坪比 1.58–1.68 正常 |
| L5 | 開發評效 | `評效 = 總銷 / 總成本`（>5 優良 / 2–5 可行 / <2 偏低） |
| L6 | 都更全案投報 | 總銷 → 共同負擔六大科目 → 共負比 / 地主分回 / 報酬率 |

**L6 共同負擔六大科目**（都市更新權利變換實施辦法）：

| 科目 | 計算方式 |
|------|---------|
| A 工程費用 | 營造費 + 設計監造（營造×5%）+ 工程管理（營造×3%） |
| B 管維費用 | A × 1%（竣工後設施維運） |
| C 權變費用 | 房地銷×1.5% + 戶數×拆補費 + 戶數×月租×月數 |
| D 貸款利息 | 土融利息 + 建融利息 |
| E 稅捐 | 房地銷×5%（營業稅）+ 總銷×0.1%（印花） |
| F 管理費用 | 總銷×5%（實施者服務費） |
| **合計** | `共同負擔 = A+B+C+D+E+F` |

---

## app.py 函式目錄

### 計算層（純 Python，不碰 Streamlit，Lines 33–298）

| 函式 | 行號 | 說明 |
|------|------|------|
| `calc_容積查核(參數, 樓層records)` | 36–87 | 逐層 §162 免計查核，回傳 FA/允建/超出/餘量 |
| `calc_坪效(允建容積, 陽台免計面積, 公設比, 外皮係數)` | 90–100 | 室內坪→銷售坪→銷坪比 |
| `calc_開發評效(銷售坪數, 成本dict)` | 103–114 | 總銷/總成本/評效比（快篩） |
| `calc_總銷(銷售坪數, 住宅單價, 店舖坪數, 店舖單價, 車位數, 車位單價)` | 123–134 | 住宅+店舖+車位三項分拆 |
| `calc_共同負擔(總銷, 房地總銷, 營造坪數, p)` | 137–177 | L6 六大科目（§辦法），回傳各科目明細 |
| `calc_分回(總銷, 共同負擔)` | 180–192 | 共負比、地主分回、報酬率 |
| `calc_投報全案(銷售坪數, 營造坪數, p)` | 195–200 | 串接總銷→共負→分回，回傳合併 dict |
| `calc_投報敏感度(銷售坪數, 營造坪數, p, 售價變動, 營造變動)` | 203–216 | 報酬率×(售價±10% × 營造±10%)矩陣 |
| `範本參數` dict | 232–252 | 合成案例預設值（案例A/B/C/D，去識別化） |
| `範本樓層表(鍵)` | 255–267 | 各案逐層樓板面積種子資料 |
| `解析上傳(file)` | 270–297 | 解析 Excel/CSV，彈性欄名對應 |
| `產生報告(...)` | 300–367 | Markdown 報告（容積帳+坪效+財務+投報） |

### UI 層（Streamlit，Lines 370–848）

| 元件 | 行號 | 說明 |
|------|------|------|
| `_role_card(...)` | 381–391 | 四角色快覽卡（HTML 渲染） |
| `main()` | 394–848 | 主程式入口 |
| Sidebar L1 輸入 | 395–520 | 基地容積/免計基準/成本/都更全案投報 |
| 逐層表格 + 上傳 | 521–590 | `st.data_editor` + 解析 `解析上傳` |
| KPI metrics | 591–640 | 允建容積/計入容積/超出/銷售坪/評效 |
| Tab ① 容積帳 | 641–680 | 儀表盤 + 合規/超出判斷表 |
| Tab ② 免計查核 | 681–710 | 逐層梯廳/陽台超出明細表 |
| Tab ③ 坪效分析 | 711–740 | 銷坪比推導步驟 + 公設比×售價敏感度 |
| Tab ④ 財務評效 | 741–780 | 成本瀑布圖 + 各科目明細 |
| Tab ⑤ 都更全案投報 | 781–825 | 六科目分拆 + 售價×營造雙軸敏感度熱力圖 |
| Tab ⑥ 報告匯出 | 826–848 | Markdown + CSV 下載 |

### 預設費率參數（`財務率預設`，Line 224–230）

| 參數 | 預設值 | 說明 |
|------|--------|------|
| 設計監造費率 | 5% | 佔營造費 |
| 工程管理費率 | 3% | 佔營造費 |
| 管維費率 | 1% | 佔工程費 A |
| 建融利率 | 3.25% | 年息 |
| 建融年期 | 3 年 | |
| 稅捐率（營業稅） | 5% | 佔房地銷 |
| 稅捐率（印花） | 0.1% | 佔總銷 |
| 管理費率 | 5% | 佔總銷（實施者） |
| 拆補費 | 9 萬/戶 | |
| 租金補貼 | 2,000/月×42月 | 都更期間 |

---

## 六大踩坑點（程式須防呆）

1. **梯廳 §162-1 逐層**：是各層樓板 × 5% 或 8%，**不是** FA×8% 總量
2. **陽台超出必須補計入**：超出 10% 的部分計入容積，不可寫 0
3. **基地面積用使照面積**：非謄本面積（實案曾見謄本與使照差約 10%，用錯會墊高免計上限）
4. **三來源比對**：建築師/代書/建設公司數字需在「圖說驗核區」比對
5. **B1F 防空避難室排除**：排除樓地板合計（§117）
6. **安全梯 15% 基準是「允建容積」**：非 FA

---

## 黃金測試（須通過 `python test_golden.py`）

> ⚠️ 去識別化（合併前置 P0-1）：黃金測試已改為**合成案例 A–D**（虛構輸入→決定性輸出，
> 純迴歸鎖定）。費率/公式曾以真實案私下校準（±5% 內），校準紀錄不進版控。

| 案 | 關鍵期望值（容差 0.5 m²） |
|----|--------------------------|
| 案例A（都更・全案管理） | FA 4480.00、允建 7616.00、計入 7616.20、餘量 −0.20（打滿邊界） |
| 案例B（危老） | 允建 4177.92、梯廳超出 30.80、計入 4210.80、餘量 −32.88 |
| 案例C（防災都更） | FA 2160.00、允建 4017.60、**陽台超出 29.40**（逐層法）、梯廳超出 2.12、計入 4049.52、餘量 −31.92 |
| 案例D（危老・合建） | 允建 6120.00、計入 6157.50、餘量 −37.50 |
| L6 鎖定 | 案例D 共負比 66.00%／報酬率 51.50%；案例A 共負比 55.92%／報酬率 78.82% |

> ⚠️ 歷史修正教訓（詳見 `REVIEW.md`，數字已去識別化）：
> 1. 「獎勵率」應以「圖說獎勵容積 ÷ FA」精確反推，不可用 spec 粗值。
> 2. 「陽台超出」必須用**逐層法**（各層樓板×10%），FA 總量法會高估約 10 倍（§162 與圖說皆為逐層）。

**待建築師確認的兩個參數**（不影響現有測試，但影響未來真實案件）：
- 梯廳免計基準：5% 還是 8%？
- 陽台免計基準：§162 的 10% 還是 §162-3 的 15%？

---

## 程式撰寫規範

- 計算邏輯獨立成函式（`calc_*`），不碰 Streamlit，方便 `test_golden.py` 直接呼叫
- 變數用中文，函式名中英混用（如 `calc_坪效`）
- 每個計算區塊加中文註解，標明對應層級（L2/L3…）與法規條文
- 狀態色：超出 → `st.error()`（紅）、接近 → `st.warning()`（黃）、合規 → `st.success()`（綠）
- 數字顯示小數 2 位，面積 m²、坪數「坪」、金額「萬元」
- 絕不寫死結果，全部從 L1 輸入推導
- 新增計算函式後，更新 `test_golden.py` 並確認通過

---

## UI 重要細節

### 四角色快覽卡（Lines 381–391）

首頁顯示四個彩色卡片，分別對應：
- 🏗️ **建管** — 容積合規狀態
- 📐 **建築師** — 免計查核結果
- 📋 **地政士** — 容積帳數字
- 💰 **開發商** — 投報率 / 評效

### 营造坪基準切換（v3.1 新增）

Sidebar 有切換鈕：「**前期保守**（用銷售坪）」vs「**實務**（用允建坪）」。
若用銷售坪當營造基準，成本可能被高估達 60%（因含公設），需注意。

### 逐層表格

`st.data_editor` 讓用戶逐層輸入或匯入：
- 可上傳 `.xlsx` / `.csv`（`解析上傳` 函式處理彈性欄名）
- 可選合成範本（案例A/B/C/D）快速帶入種子資料
- 每行：樓層、樓板面積(m²)、圖說計入容積、梯廳、安全梯、陽台

---

## Excel 對照範本

`都更全案投報_對照範本.xlsx`（由 `make_template.py` 產生）：
- **Sheet 1「坪效及獲利分析」**：黃色=輸入格、藍色=關鍵指標，公式與 `calc_投報全案` 1:1 對齊
- **Sheet 2「參數來源與說明」**：法規依據、費率假設、使用說明

每次修改 `calc_共同負擔` 等 L6 函式後，需重跑 `python make_template.py` 同步範本。

---

## Agent 框架

1. **撰寫**：在 `claude/claude-md-docs-ls9Bu` 分支開發
2. **審查**：見 `REVIEW.md`（Agent 2 獨立審查公式 + 法規）
3. **部署**：PR 合併到 `main` → Streamlit Cloud 自動更新

---

## Git 規範

- **開發分支**：`claude/claude-md-docs-ls9Bu`
- **穩定/部署分支**：`main`（對應 Streamlit Cloud 自動部署）
- Commit message 用中文或英文均可，格式：`feat:` / `fix:` / `docs:` / `refactor:`
- 每次修改計算公式**必須**先跑 `python test_golden.py` 確認通過才 commit

---

## 敏感資料

- `供py/` 資料夾：含真實案件 Excel，已加入 `.gitignore`，**絕對不可 commit**
- `.streamlit/config.toml`：僅含 UI 主題，無機密

---

---

## 部署後驗收清單（每次 main 合併後必做，30 秒）

> 目的：確認「程式碼正確」≠「用戶看到的正確」。以下 4 項任何一項失敗，都要先修再做下一個需求。

打開 Streamlit Cloud 的部署 URL，執行：

```
□ 頁尾顯示的 build 日期是今天的 commit 日期？（確認部署已更新）
□ Sidebar 所有步驟標題（Step 1~4）emoji 與文字正常顯示？
□ 載入「案例C」範本 → L2→L6 流程帶數字出現？
□ Tab ① 點開 → 三項免計核對表有資料列？
```

---

## 建築師反饋收集協議（建築師說「有出入」時，先問這三個問題再改 code）

> 目的：不把症狀直接轉發給 AI——先把症狀解讀成具體的輸入/輸出差異，才能精準修正。

收到建築師反饋後，先收集以下資訊再開新對話：

```
Q1：他在哪個步驟卡住？
    → 是「找不到輸入欄位」、「數字算出來不對」、還是「結果格式看不懂」？

Q2：他的實際操作是什麼？
    → 例：他有沒有從 Excel 面積表匯入？他是手動輸入還是直接填圖說數值？
    → 他在建築師事務所用的欄位名稱叫什麼（梯廳還是走廊？陽台還是露台？）？

Q3：他期望看到什麼，我們工具給他看到什麼？
    → 把兩個數字或兩個畫面截圖附上。
```

拿到這三個答案後，才能精準告訴 AI 要改什麼。

---

## Git 工作流程（直接 push，不開 PR）

> PR 在這個專案只有一個 reviewer（自己），沒有審查價值，改為直接推送。

```bash
# 標準流程（每次改完 test_golden.py 全 PASS 後）
python test_golden.py                          # 必須全 PASS 才能 commit
git add app.py                                 # 只加改過的檔案，不 git add -A
git commit -m "fix/feat/docs: 說明改了什麼"
git push origin claude/claude-md-docs-ls9Bu   # 推到開發分支

# 要上線時（直接 push main，不開 PR）
git checkout main
git merge claude/claude-md-docs-ls9Bu --no-ff -m "merge: v4.x 說明"
git push origin main
git checkout claude/claude-md-docs-ls9Bu      # 切回繼續開發
```

> 例外：有需要外部審查（建築師確認計算邏輯）時才開 PR，正常開發直接 push。

---

## 版本號更新規則（v4.4 起）

每次改 `app.py` 時，同步更新以下兩處，部署後才能從頁尾確認版本：

```python
# 頁尾（app.py 最後幾行）
_build = "YYYY-MM-DD"          # 改成今天的日期
'RE-DCF-Tool v4.X'             # 版本號 +0.1

# Hero 橫幅
v4.X                           # 同步更新
```

---

## 第二階段（先不做）

住宅工具上線後，複製架構做商辦版（NOI + Cap Rate + 持有期報酬），另開對話處理。
